package sort.SortsPart2;

public class MSDBinaryDigits {

    private static final int m = 3;

    public static void main(String[] args) {

    }


    private static void MSD(int[] A, int l, int r, int d) {

        /*if (d > m || l >= r)
        return;
        for (int j = 0; j < k + 1; j++){
            cnt[j] = 0
        }

        for i = l to r
        j = digit(A[i], d)
        cnt[j + 1]++
        for j = 2 to k
        cnt[j] += cnt[j - 1]
        for i = l to r
        j = digit(A[i], d)
        c[l + cnt[j]] = A[i]
        cnt[j]++
        for i = l to r
        A[i] = c[i]
        radixSort(A, l, l + cnt[0] - 1, d + 1)
        for i = 1 to k
        radixSort(A, l + cnt[i - 1], l + cnt[i] - 1, d + 1)
    }*/
        System.out.println("s");
    }
}
